mixin bookCard(book = {})
  .book
    .book__hero
      .book__actions
        button Soon...
      h2.title
        a(href=`/books/${book.id}`) #{book.title}
    .book__details
      p #{book.author}
      if book.summary
        p= book.summary.split(" ").slice(0, 25).join(" ")
